"""jmd-commons — shared utilities for the JMD ecosystem.

Currently exposes the :mod:`jmd_commons.paths` module — canonical
MSYS2-form path handling that's the source of truth for every
JMD MCP server on Windows. See :doc:`README` for the longer-term
roadmap (security, credentials, ...).
"""

__version__ = "0.1.0"
